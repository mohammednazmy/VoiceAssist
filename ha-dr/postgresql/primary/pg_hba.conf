# PostgreSQL Primary Server Access Control (Phase 12 - HA/DR)
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Allow replication connections from replica
host    replication     voiceassist     0.0.0.0/0               md5
host    replication     all             0.0.0.0/0               md5

# Allow application connections
host    all             voiceassist     0.0.0.0/0               md5
host    all             all             0.0.0.0/0               md5
