<VirtualHost *:80>
    ServerName assistdocs.asimo.io
    ServerAdmin admin@asimo.io

    # Enforce HTTPS and canonical docs domain
    RewriteEngine On
    RewriteRule ^/(.*)$ https://docs.asimo.io/$1 [R=301,L]

    ErrorLog ${APACHE_LOG_DIR}/assistdocs-error.log
    CustomLog ${APACHE_LOG_DIR}/assistdocs-access.log combined
</VirtualHost>

<VirtualHost *:443>
    ServerName assistdocs.asimo.io
    ServerAdmin admin@asimo.io

    SSLEngine on
    # SSL certificates will be managed by certbot

    # Enforce canonical host over HTTPS as well
    RewriteEngine On
    RewriteRule ^/(.*)$ https://docs.asimo.io/$1 [R=301,L]

    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"

    ErrorLog ${APACHE_LOG_DIR}/assistdocs-error.log
    CustomLog ${APACHE_LOG_DIR}/assistdocs-access.log combined
</VirtualHost>
