# Development Environment Configuration (local-only)
# Always point to local services to avoid hitting remote asimo.io endpoints.
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/api/realtime/ws

# Enable unified chat/voice UI
VITE_UNIFIED_UI=true
