# Production Environment Configuration (local-only baseline)
# These defaults intentionally target local services to avoid hitting remote hosts.
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/api/realtime/ws

# Enable unified chat/voice UI
VITE_UNIFIED_UI=true

# Cross-app navigation URLs (local admin/docs)
VITE_ADMIN_URL=http://localhost:5174
VITE_DOCS_URL=http://localhost:3001

# Silero VAD assets
# For local dev and default Docker builds we intentionally rely on the
# upstream CDN URLs baked into useSileroVAD. This keeps the behavior
# consistent with the Vite dev server (5173) and avoids subtle MIME /
# module-loading issues when self-hosting ONNX/Silero assets via nginx.
# When deploying to tightly locked-down environments (e.g. hospital
# networks), set these via environment variables at deploy time:
#   VITE_SILERO_ONNX_WASM_BASE_URL=/vendor/onnxruntime-web/dist/
#   VITE_SILERO_VAD_ASSET_BASE_URL=/vendor/silero-vad/
